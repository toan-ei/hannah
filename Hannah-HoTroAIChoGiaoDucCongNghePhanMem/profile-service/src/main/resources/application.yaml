server:
  port: 8081
  servlet:
    context-path: /profile

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/profile_service"
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
