server:
  port: 8082
  servlet:
    context-path: /post
spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/post-service?authSource=admin
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
