server:
  port: 8082
  servlet:
    context-path: /post
spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/post-service?authSource=admin
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
